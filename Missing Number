class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int n=nums.size();
        long long int total = (long long int)n*(n+1)/2;
        long long int sum = 0;
        for(int i=0;i<n;i++) {
            sum+=nums[i];
        }
        return total-sum;
    }
};
